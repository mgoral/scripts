A simple script checking for updates. Should be put e.g. in /usr/local/bin and scheduled in anacron:

# /etc/anacrontab
# period   job delay (minutes)   job id   command
@weekly   20   checkupdate   nice check-updates virgo
